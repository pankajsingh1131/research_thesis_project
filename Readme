Steps to run the code:

1. install the requirement packages from requirements.txt
        pip install -r requirements.txt

2. Download the GoogleNews-vectors-negative300.bin.gz from (https://github.com/mmihaltz/word2vec-GoogleNews-vectors)
   Extract the contents at the same directory level as propose_model.ipynb file

3. Run jupyter lab or notebook using either of the following commands in the terminal
   command 1:
   jupyter notebook
   command 2:
   jupyter lab
